import React from 'react';
import { Section, SectionDivider, SectionTitle } from '../../styles/GlobalComponents';

const AllProjects = () => (
  <Section>
    <SectionDivider />
    <SectionTitle >What I've been up to</SectionTitle>
  </Section>
);

export default AllProjects;